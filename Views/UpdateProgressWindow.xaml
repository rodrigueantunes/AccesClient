<Window x:Class="AccesClientWPF.Views.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="#EAEDED"
        ShowInTaskbar="True">
    <Border Background="White" CornerRadius="10" Padding="18" Width="560">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#34495E"/>

            <TextBlock Grid.Row="1" Margin="0,10,0,0"
                       Text="{Binding Status}" TextWrapping="Wrap"
                       Foreground="#34495E" FontSize="13"/>

            <ProgressBar Grid.Row="2" Margin="0,12,0,0" Height="18"
                         Minimum="0" Maximum="100"
                         Value="{Binding ProgressPercent}"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="Annuler"
                        Width="110" Height="36"
                        Background="#E74C3C" Foreground="White"
                        BorderThickness="0"
                        IsEnabled="{Binding CanCancel}"
                        Click="Cancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>