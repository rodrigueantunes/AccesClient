<Window x:Class="AccesClientWPF.Views.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="#EAEDED"
        ShowInTaskbar="False">
    <Border Background="White" CornerRadius="10" Padding="18" Width="520">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="{Binding Title}"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#34495E"/>

            <!-- Message -->
            <Border Grid.Row="1"
                    Margin="0,12,0,14"
                    Background="#F7F9F9"
                    CornerRadius="8"
                    Padding="12">
                <TextBlock Text="{Binding Message}"
                           TextWrapping="Wrap"
                           Foreground="#34495E"
                           FontSize="13"/>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Plus tard"
                        Width="110"
                        Height="36"
                        Margin="0,0,10,0"
                        Background="#34495E"
                        Foreground="White"
                        BorderThickness="0"
                        Click="Later_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="10">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Mettre à jour"
                        Width="130"
                        Height="36"
                        Background="#2ECC71"
                        Foreground="White"
                        BorderThickness="0"
                        Click="Update_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="10">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>

        </Grid>
    </Border>
</Window>