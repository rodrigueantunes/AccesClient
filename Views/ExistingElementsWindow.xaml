<Window x:Class="AccesClientWPF.Views.ExistingElementsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:AccesClientWPF.Converters"
        Title="Éléments existants"
        Width="700" Height="550"
        Background="#EAEDED"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <conv:FileTypeToIconConverter x:Key="FileTypeToIconConverter"/>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#5DADE2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#34495E" CornerRadius="10" Padding="12" Margin="0,0,0,10">
            <DockPanel>
                <TextBlock Text="Client :" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding ClientName}" Foreground="White" FontSize="16"/>
            </DockPanel>
        </Border>

        <!-- Tree : Racine + Rangements -->
        <Border Grid.Row="1" Background="#FDFEFE" CornerRadius="10" Padding="10" BorderBrush="#D5DBDB" BorderThickness="1">
            <TreeView x:Name="Tree"
                      ItemsSource="{Binding Nodes}"
                      SelectedItemChanged="Tree_SelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="18" Height="18" Margin="0,0,8,0"
                                   Source="{Binding File, Converter={StaticResource FileTypeToIconConverter}}"/>
                            <TextBlock Text="{Binding Display}" FontWeight="{Binding FontWeight}" Foreground="#2C3E50"/>
                            <TextBlock Text="{Binding TypeText}" Foreground="#7F8C8D" Margin="10,0,0,0"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="Ajouter" Width="110" Margin="5" Click="AddElement_Click" Style="{StaticResource RoundedButton}"/>
            <Button Content="Modifier" Width="110" Margin="5" Click="EditElement_Click" Style="{StaticResource RoundedButton}"/>
            <Button Content="Supprimer" Width="110" Margin="5" Click="DeleteElement_Click" Style="{StaticResource RoundedButton}"/>
            <Button Content="Fermer" Width="110" Margin="5" Click="Close_Click" Style="{StaticResource RoundedButton}"/>
        </StackPanel>
    </Grid>
</Window>